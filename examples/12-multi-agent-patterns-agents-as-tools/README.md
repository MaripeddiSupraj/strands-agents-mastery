# Example 12 — Multi-Agent: Agents as Tools

## Goal

Split one large Agent into an Incident Commander plus two specialists.

## Run

```bash
pip install strands-agents
python main.py
```

Observe which specialist the commander calls and the extra model/token cost introduced by delegation.
